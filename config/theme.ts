import type { ThemeConfig } from 'antd';
import { theme as atheme } from 'antd';

const theme: ThemeConfig = {
  algorithm: atheme.defaultAlgorithm
  //token: {
  //  fontSize: 16,
  //  colorPrimary: '#52c41a',
  //},
};

export default theme;